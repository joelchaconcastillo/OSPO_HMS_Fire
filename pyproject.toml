[build-system]
requires = ["flit_core >=3.2,<4", "basemap"]
build-backend = "flit_core.buildapi"

[project]
name = "HMS_Fire"
authors = [{name = "Graziella", email = "graziella@lumache"}]
dynamic = ["version", "description"]
